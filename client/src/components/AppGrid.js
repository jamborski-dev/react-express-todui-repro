export const AppGrid = ({ children }) => {
  return <main className="app-grid">{children}</main>
}

export const TopBar = ({ children }) => {
  return <header className="topbar">{children}</header>
}
